# Fito Investments — Preview App

This is a minimal Next.js frontend (Dark Premium, neon-blue accents) used to preview the UI.
It is a frontend-only mock that points to a backend API at NEXT_PUBLIC_API_BASE.

To preview locally (optional):
1. npm install
2. npm run dev
